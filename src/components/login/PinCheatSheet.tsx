import React from 'react';

/**
 * PinCheatSheet is disabled in production to prevent exposing
 * committee PIN codes to participants or public users.
 */
export const PinCheatSheet: React.FC = () => {
  return null;
};
